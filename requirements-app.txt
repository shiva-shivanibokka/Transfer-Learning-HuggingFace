# Slim dependency set for the Hugging Face Space (inference only).
# Training-only deps (mlflow, umap-learn, albumentations, opencv, notebook,
# plotly, seaborn) are intentionally excluded to keep the Space build fast.
# torch floor is 2.6: transformers>=4.53 blocks loading legacy .bin checkpoints
# on torch<2.6 (CVE-2025-32434). The CLIP tab uses openai/clip-vit-base-patch32,
# whose Hub weights are .bin only — so the Space itself needs torch>=2.6.
torch>=2.6.0,<3.0.0
torchvision>=0.21.0,<1.0.0
transformers>=4.47.0,<5.0.0
huggingface-hub>=0.22.0,<1.0.0
Pillow>=11.4.0,<13.0.0  # 12.x fixes 5 Pillow CVEs flagged by pip-audit
numpy>=1.26.0,<3.0.0  # allow numpy-2 ABI (pyarrow>=18 / torch 2.6 need it)
scipy>=1.13.0,<2.0.0
scikit-learn>=1.4.0,<2.0.0
matplotlib>=3.8.0,<4.0.0
pandas>=2.2.0,<3.0.0
tabulate>=0.9.0,<1.0.0
gradio>=4.36.0,<7.0.0
# Explicit: the Dockerfile CMD runs the uvicorn binary and the app mounts a
# FastAPI JSON API (app/api.py). Both are gradio deps, but pin them directly.
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
pydantic>=2.0,<3.0
